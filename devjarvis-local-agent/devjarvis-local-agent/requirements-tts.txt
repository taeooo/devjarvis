# Optional local TTS runtime.
# DevJarvis uses this only on the user's local PC through the loopback Local Agent.
# Install this file only when high-quality local voice output is needed.
#
# Recommended quality provider for a more natural local assistant voice:
#   DEVJARVIS_LOCAL_AGENT_TTS_PROVIDER=cosyvoice2_local
#
# Lightweight fallback provider kept for compatibility:
#   DEVJARVIS_LOCAL_AGENT_TTS_PROVIDER=melotts_kr
#
# CosyVoice2 is installed from its project repository in the setup script because
# it needs its own repo assets/submodules and a downloaded model directory.

# MeloTTS fallback runtime. Keep optional and local-only.
git+https://github.com/myshell-ai/MeloTTS.git@main

# Shared helpers used by CosyVoice2 setup / runtime checks.
huggingface_hub>=0.23.0
